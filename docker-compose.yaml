services:
  bodyclocks:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bodyclocks
    ports:
      - "7860:7860"
    environment:
      R_CONFIG_ACTIVE: docker
    restart: unless-stopped